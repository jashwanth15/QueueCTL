"""
queuectl.py — Convenience wrapper so you can run:
    python queuectl.py <command> [options]
from the project root directory.
"""
from queuectl.cli import main

if __name__ == "__main__":
    main()
